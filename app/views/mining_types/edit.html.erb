<h1><%= t('links.edit') %> <%= MiningType.model_name.human(count: 1)%></h1>

<%= render 'form', mining_type: @mining_type %>

<%= link_to t('links.show'), @mining_type %> |
<%= link_to t('links.back'), mining_types_path %>
